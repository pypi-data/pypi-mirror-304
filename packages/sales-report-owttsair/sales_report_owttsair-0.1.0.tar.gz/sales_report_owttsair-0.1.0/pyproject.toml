[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sales-report-owttsair"
version = "0.1.0"
description = "A package for generating sales reports from CSV files."
authors = [
    { name = "Колесникова Анастасия", email = "kolesnikova20051003@gmail.com" }
]
dependencies = ["pandas"]

[tool.setuptools]
packages = ["sales_report"]
