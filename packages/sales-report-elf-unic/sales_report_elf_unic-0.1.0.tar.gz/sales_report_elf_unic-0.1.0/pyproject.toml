[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = 'sales_report_elf_unic'
version = '0.1.0'
dependencies = ['pandas==2.2.3']

description = 'count sales and quantity'
authors =  [{name='Varvara'}]
requires-python = ">=3.8"

[project.scripts]
sales_data = 'sales_data:main'
[tool.setuptools]
py-modules = ['sales_data']
