[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "transaction_report_12123123"
version = "0.1.0"
description = "A package to generate financial reports from transaction data."
authors = [{ name = "Vlad", email = "abvvladivanov@gmail.com" }]
dependencies = ["pandas"]
