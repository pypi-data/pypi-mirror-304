[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "financial_report_by_podpivas"
version = "0.1.0"
description = "Python-пакет для анализа доходов и расходов компании."
authors = [
    { name="podpivas", email="k.shaykhraziev@edu.centraluniversity.ru" }
]
dependencies = ["pandas"]

[project.scripts]
financial_report = "financial_report.main:main"

