+ keysearch for hf models?

bugs:

NICE TO HAVE:
+ improve interface of model processor to prevent DRY -> pydantic? dataclasses?
+ add voicemodel to speak prompts instead of writing them for faster interaction
