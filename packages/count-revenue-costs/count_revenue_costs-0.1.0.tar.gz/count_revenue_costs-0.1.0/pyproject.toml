[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "count_revenue_costs"
version = "0.1.0"
description = "Подсчет доходов и расходов"
authors = [{ name = "Andrey"}]
dependencies = ["pandas"]
license = {text = 'MIT'}
readme = 'README.md'

[project.scripts]
rev_costs = "count_revenue_costs.main:main"