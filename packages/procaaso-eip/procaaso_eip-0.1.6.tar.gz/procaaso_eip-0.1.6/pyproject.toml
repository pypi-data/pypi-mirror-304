[tool.poetry]
name = "procaaso-eip"
version = "0.1.6"
description = ""
authors = ["Brandon Smith <bsmith@consynsys.com>"]
readme = "README.md"

packages = [
	{ include = "eip", from = "./"},
	{ include = "eds", from = "./"},
]

[tool.poetry.dependencies]
python = "^3.10"
pydantic = ">=1.10.9,<2.0.0"
pyyaml = "^6.0.1"


click = "^8.1.7"
[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
eds-parser = "eds.parser:main"