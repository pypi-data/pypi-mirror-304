[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = 'customer_analisis_elf_unic'
version = '0.1.0'
dependencies = ['pandas==2.2.3']

description = 'analise customers'
authors =  [{name='Varvara'}]
requires-python = ">=3.8"

[project.scripts]
customer_analisis = 'customer_analisis:main'
[tool.setuptools]
py-modules = ['customer_analisis']
