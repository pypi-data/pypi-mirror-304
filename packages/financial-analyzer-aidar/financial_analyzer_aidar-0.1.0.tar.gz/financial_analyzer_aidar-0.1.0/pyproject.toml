[project]
name = "financial_analyzer_aidar"
version = "0.1.0"
authors = [
  { name = "Aidar", email = "Falnker.b27@yandex.ru" }
]
description = "Пакет для анализа финансовых транзакций."
readme = "README.md"
requires-python = ">=3.7"

[tool.setuptools]
packages = []

[tool.poetry]
name = "financial_analyzer_aidar"
version = "0.1.0"
description = "Пакет для анализа финансовых транзакций."
authors = ["Aidar <Flanker.b27@yandex.com>"]
readme = "README.md"
license = "MIT"
dependencies = [
  "pandas>=1.0.0"
]

[build-system]
requires = ["setuptools>=57.0.0", "wheel"]
build-backend = "setuptools.build_meta"