[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "finance-report-owttsair"
version = "0.1.0"
description = "A package for generating financial reports from CSV files."
authors = [
    { name = "Анастасия Колесникова", email = "kolesnikova20051003@gmail.com" }
]
dependencies = ["pandas"]

[tool.setuptools]
packages = ["finance_report"]
