[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = 'cringeHW3'
version = '0.0.1'
dependencies = [
    'pandas',
]
description = 'It cli app group transaction by category'
authors =  [{ name = "osisochka", email = "a.onufrienko@edu.centraluniversity.ru" }]
requires-python = '>3.8'

[project.scripts]
sales-report = "sales_report:main"

#[tool.setuptools]
#py-modules =
