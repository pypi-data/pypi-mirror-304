def add(
    x: float,
    y: float
) -> float:
    """
    Adding two numbers together.

    Args:
        x (float): First number to add.
        y (float): Second number to add.
    Returns:
        float: Addition of x and y together.
    """
    return x + y