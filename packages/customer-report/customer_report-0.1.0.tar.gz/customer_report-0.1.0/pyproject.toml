[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "customer_report"
version = "0.1.0"
description = "Пакет для cоздания отчёта о клиентах"
authors = [
    { name = "mikhail", email = "m.zimin@edu.centraluniversity.ru" }
]
dependencies = [
    "argparse"
]