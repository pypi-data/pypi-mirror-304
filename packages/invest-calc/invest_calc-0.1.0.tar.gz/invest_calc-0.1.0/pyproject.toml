[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "invest_calc"
version = "0.1.0"
description = "Пакет для вычисления ROI и чистой прибыли"
authors = [
    { name = "mikhail", email = "m.zimin@edu.centraluniversity.ru" }
]
dependencies = [
    "argparse"
]