[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = 'menegment_elf_unic'
version = '0.1.0'
dependencies = []

description = 'Управление заказами и учетными записями'
authors = [{name='Varvara'}]
requires-python = ">=3.8"
readme = 'README.md'

[project.scripts]
__main__ = '__main__:main_menu'
[tool.setuptools]
py-modules = ['order_managment', 'user_menagment']
