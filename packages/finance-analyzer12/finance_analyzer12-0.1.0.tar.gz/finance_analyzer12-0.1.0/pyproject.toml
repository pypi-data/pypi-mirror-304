[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "finance-analyzer12"
version = "0.1.0"
description = "Пакет для анализа финансовых транзакций."
authors = [{ name = "Nikita1207", email = "dorogin345@gmail.com" }]
dependencies = ["pandas"]

[project.urls]
