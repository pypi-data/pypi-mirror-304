[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pussy"
version = "0.1.1"
description = "A humorous library for finding the clit."
authors = [
    { name = "420Rentner69" }
]
readme = "readme.md"
dependencies = []

[tool.setuptools.packages]
find = { include = ["*"] }
