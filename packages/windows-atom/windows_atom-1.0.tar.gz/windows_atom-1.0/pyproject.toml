[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "windows_atom"
version = "1.0"
description = "A Python library to interact with the Windows atom table via ctypes."
readme = "README.md"
authors = [
    { name = "Mohamed Adil", email = "MohamedAdilOfficial@gmail.com" }
]
license = { text = "MIT" }
requires-python = ">=3.6"
dependencies = []

[project.urls]
Homepage = "https://github.com/Mohamed-Adil-Cyber/windows_atom"
