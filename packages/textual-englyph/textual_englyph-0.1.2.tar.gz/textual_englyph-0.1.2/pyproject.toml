[project]
name = "textual-englyph"
version = "0.1.2"
description = "A Textual widget to generate stylized large text."
readme = "README.md"
license = {text = "MIT License"}
requires-python = ">=3.12"
dependencies = [
    "pillow>=10.4.0",
    "textual>=0.82.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
