# setup.py

from setuptools import setup, find_packages

setup(
    name='TurboTalk_Custom',
    version='0.1.5',
    packages=find_packages(),
    install_requires=[
        'g4f'  # Include any dependencies here
    ],
)
