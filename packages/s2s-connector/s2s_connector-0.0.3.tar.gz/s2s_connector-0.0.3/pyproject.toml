[tool.poetry]
name = "s2s-connector"
version = "0.0.3"
description = "A general-purpose Service-to-Service (S2S) connector for obtaining bearer tokens for authentication in service-to-service communication."
authors = ["Pavel Mulin <mulin.pasha@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.9.0"
httpx = ">=0.23.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"